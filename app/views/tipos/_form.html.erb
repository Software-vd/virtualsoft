<center><%= simple_form_for @tipo, :html => { :class => 'form-horizontal' } do |f| %></center>
  <%= f.input :Descripcion %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-success' %>
    <%= link_to t('.cancel', :default => t("helpers.links.Cancelar")),
                tipos_path, :class => 'btn btn-inverse' %>
  </div>
<% end %>
