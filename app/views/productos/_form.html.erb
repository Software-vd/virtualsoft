<%= simple_form_for(@producto, :html => { :class => 'form-horizontal' }, :remote => true) do |f| %>
  <%= f.label :tamano_id %>:::
  <%= f.collection_select :tamano_id, Tamano.all, :id, :descripcion, prompt:"tamaño",  :required => true %>
  <br>
  <br>
  <%= f.label :tipo_id %>:::
  <%= f.collection_select :tipo_id, Tipo.all, :id, :Descripcion, prompt:"tipo",  :required => true %>
  <br>
  <br>
  <%= f.input :descripcion, :label => "Descripcion",  :required => true %>
  <%= f.input :valor, :label => "Valor",  :required => true %>
  <center>
    <%= f.submit "guardar", :class => 'btn btn-info' %>
    <%= link_to 'Atras', productos_path, :class => 'btn btn-inverse' %>
  </center>
<% end %>
