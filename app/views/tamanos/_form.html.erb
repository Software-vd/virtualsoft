<%= simple_form_for(@tamano, :html => { :class => 'form-horizontal' }, :remote => true) do |f| %>

  <%= f.input :descripcion, :label => "Descripcion",  :required => true %>

  <div class="form-actions">
    <%= f.submit "guardar", :class => 'btn btn-info' %>
    <%= link_to 'Atras', tamanos_path, :class => 'btn btn-inverse' %>
  </div>
<% end %>


