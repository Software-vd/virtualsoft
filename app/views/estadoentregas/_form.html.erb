<%= simple_form_for(@estadoentrega, :html => { :class => 'form-horizontal' }, :remote => true) do |f| %>

  <%= f.input :descripcion, :label => "Descripcion",  :required => true %>
 
   <div class="form-actions">
    <%= f.submit "guardar", :class => 'btn btn-info' %>
    <%= link_to 'Atras', estadoentregas_path, :class => 'btn btn-inverse' %>
  </div>
<% end %>
 