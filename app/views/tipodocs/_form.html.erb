<center><%= simple_form_for @tipodoc, :html => { :class => 'form-horizontal' } do |f| %></center>
  <%= f.input :Sigla %>
  <%= f.input :cliente_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-success' %>
    <%= link_to t('.cancel', :default => t("helpers.links.Cancelar")),
                tipodocs_path, :class => 'btn btn-inverse' %>
  </div>
<% end %>
